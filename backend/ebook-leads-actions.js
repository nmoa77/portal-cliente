const crypto=require('crypto');
const fs=require('fs');
const path=require('path');
const express=require('express');
const bcrypt=require('bcryptjs');
const db=require('./db');
const {requireAdmin}=require('./auth');
const {deliver}=require('./email');

module.exports=function installEbookLeads(app){
 const publicDir=path.join(__dirname,'..','public');
 const assetDir=process.env.DATABASE_PATH?path.join(path.dirname(process.env.DATABASE_PATH),'ebook-assets'):path.join(__dirname,'ebook-assets');
 try{fs.mkdirSync(assetDir,{recursive:true})}catch(_){}
 db.exec(`CREATE TABLE IF NOT EXISTS ebook_pages(id INTEGER PRIMARY KEY AUTOINCREMENT,slug TEXT NOT NULL UNIQUE,title TEXT NOT NULL,image_path TEXT,pdf_path TEXT,active INTEGER NOT NULL DEFAULT 1,is_favorite INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT(datetime('now')),updated_at TEXT NOT NULL DEFAULT(datetime('now')));CREATE TABLE IF NOT EXISTS ebook_page_visits(id INTEGER PRIMARY KEY AUTOINCREMENT,page_id INTEGER NOT NULL,source TEXT,campaign TEXT,created_at TEXT NOT NULL DEFAULT(datetime('now')),FOREIGN KEY(page_id) REFERENCES ebook_pages(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_ebook_visits_page ON ebook_page_visits(page_id);CREATE TABLE IF NOT EXISTS ebook_leads(id INTEGER PRIMARY KEY AUTOINCREMENT,page_id INTEGER,name TEXT NOT NULL,email TEXT NOT NULL,company TEXT,source TEXT,campaign TEXT,marketing_consent INTEGER NOT NULL DEFAULT 0,token TEXT NOT NULL UNIQUE,created_at TEXT NOT NULL DEFAULT(datetime('now')),first_downloaded_at TEXT,last_downloaded_at TEXT,download_count INTEGER NOT NULL DEFAULT 0,FOREIGN KEY(page_id) REFERENCES ebook_pages(id) ON DELETE SET NULL);CREATE INDEX IF NOT EXISTS idx_ebook_leads_email ON ebook_leads(lower(email));CREATE INDEX IF NOT EXISTS idx_ebook_leads_created ON ebook_leads(created_at);`);
 const leadCols=db.prepare(`PRAGMA table_info(ebook_leads)`).all().map(c=>c.name);if(!leadCols.includes('page_id'))db.exec(`ALTER TABLE ebook_leads ADD COLUMN page_id INTEGER`);db.exec(`CREATE INDEX IF NOT EXISTS idx_ebook_leads_page ON ebook_leads(page_id)`);
 const pageCols=db.prepare(`PRAGMA table_info(ebook_pages)`).all().map(c=>c.name);if(!pageCols.includes('is_favorite'))db.exec(`ALTER TABLE ebook_pages ADD COLUMN is_favorite INTEGER NOT NULL DEFAULT 0`);
 const clean=(v,max=500)=>String(v??'').trim().slice(0,max),validEmail=e=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e);
 function randomSlug(){let s;do{s=crypto.randomBytes(7).toString('base64url').slice(0,10)}while(db.prepare('SELECT 1 FROM ebook_pages WHERE slug=?').get(s));return s}
 let defaultPage=db.prepare(`SELECT * FROM ebook_pages ORDER BY id LIMIT 1`).get();if(!defaultPage){const r=db.prepare(`INSERT INTO ebook_pages(slug,title,image_path,pdf_path,active,is_favorite)VALUES(?,?,?,?,1,1)`).run(randomSlug(),'6 curiosidades para olhar de outra forma.','/capa.webp','/ebook-duit.pdf');defaultPage=db.prepare('SELECT * FROM ebook_pages WHERE id=?').get(r.lastInsertRowid)}
 const readable=db.prepare(`SELECT id,slug FROM ebook_pages`).all().filter(p=>!/^[A-Za-z0-9_-]{10}$/.test(String(p.slug||'')));for(const p of readable)db.prepare(`UPDATE ebook_pages SET slug=?,updated_at=datetime('now') WHERE id=?`).run(randomSlug(),p.id);defaultPage=db.prepare(`SELECT * FROM ebook_pages ORDER BY id LIMIT 1`).get();db.prepare(`UPDATE ebook_leads SET page_id=? WHERE page_id IS NULL`).run(defaultPage.id);
 if(!db.prepare(`SELECT 1 FROM ebook_pages WHERE is_favorite=1 AND active=1 LIMIT 1`).get()){const first=db.prepare(`SELECT id FROM ebook_pages WHERE active=1 ORDER BY id LIMIT 1`).get();if(first){db.prepare(`UPDATE ebook_pages SET is_favorite=0`).run();db.prepare(`UPDATE ebook_pages SET is_favorite=1 WHERE id=?`).run(first.id)}}
 try{const adminHtml=path.join(publicDir,'admin.html');let h=fs.readFileSync(adminHtml,'utf8');if(!h.includes('/js/ebook-manager.js'))h=h.replace('</body>','<script src="/js/ebook-manager.js?v=20260904m"></script>\n</body>');else h=h.replace(/ebook-manager\.js\?v=[^'\"]+/g,'ebook-manager.js?v=20260904m');fs.writeFileSync(adminHtml,h,'utf8')}catch(_){}
 try{const adminJs=path.join(publicDir,'js','admin.js');let s=fs.readFileSync(adminJs,'utf8');if(!s.includes("id: 'ebooks'"))s=s.replace("{ id: 'prospects', icon: 'sparkle', label: 'Prospects',","{ id: 'ebooks', icon: 'file', label: 'Ebook LPs' },\n    { id: 'prospects', icon: 'sparkle', label: 'Prospects',");if(!s.includes("view === 'ebooks'"))s=s.replace("else if (view === 'prospects')await viewProspects(main);","else if (view === 'prospects')await viewProspects(main);\n    else if (view === 'ebooks') await viewEbookPages(main);");fs.writeFileSync(adminJs,s,'utf8')}catch(e){console.warn('[ebook] integração admin:',e.message)}
 const portal=(process.env.PORTAL_URL||'https://cliente.duit.pt').replace(/\/+$/,'');
 const pageById=id=>db.prepare('SELECT * FROM ebook_pages WHERE id=?').get(Number(id)),pageBySlug=slug=>db.prepare('SELECT * FROM ebook_pages WHERE slug=?').get(clean(slug,100));
 function createProspectFromEbookLead({name,email,company,page,source,campaign}){const existing=db.prepare(`SELECT id,is_prospect FROM users WHERE lower(email)=lower(?) LIMIT 1`).get(email);if(existing)return{created:false,user_id:Number(existing.id),already_exists:true};const prospectCompany=clean(company,180)||clean(name,180)||'Lead Ebook',prospectName=clean(name,180)||prospectCompany,origin=['Origem: Ebook LP',`Ebook: ${clean(page?.title,220)||'Ebook DUIT'}`,`Canal: ${clean(source,120)||'direto'}`,campaign?`Campanha: ${clean(campaign,120)}`:null].filter(Boolean).join('\n'),passwordHash=bcrypt.hashSync(crypto.randomBytes(18).toString('base64url'),10);const tx=db.transaction(()=>{const u=db.prepare(`INSERT INTO users(name,email,password_hash,role,company,phone,is_prospect,is_active)VALUES(?,?,?,?,?,?,1,0)`).run(prospectName,email,passwordHash,'client',prospectCompany,null);db.prepare(`INSERT INTO prospect_crm(user_id,opportunity,lead_status,priority,notes,updated_at)VALUES(?,?,'por_contactar','possivel',?,datetime('now'))`).run(u.lastInsertRowid,'Lead captado através de ebook.',origin);return Number(u.lastInsertRowid)});return{created:true,user_id:tx(),already_exists:false}}
 try{const ls=db.prepare(`SELECT l.name,l.email,l.company,l.source,l.campaign,p.id page_id,p.title FROM ebook_leads l LEFT JOIN ebook_pages p ON p.id=l.page_id ORDER BY l.id`).all();for(const l of ls)if(validEmail(clean(l.email,180)))createProspectFromEbookLead({name:l.name,email:clean(l.email,180).toLowerCase(),company:l.company,page:{id:l.page_id,title:l.title},source:l.source,campaign:l.campaign})}catch(e){console.warn('[ebook] migração para prospects:',e.message)}
 app.get('/ebook/:slug',(req,res,next)=>{const p=pageBySlug(req.params.slug);if(!p)return next();res.redirect(301,`/${encodeURIComponent(p.slug)}${req.url.includes('?')?'?'+req.url.split('?')[1]:''}`)});
 app.get('/:slug',(req,res,next)=>{const p=pageBySlug(req.params.slug);if(!p||!p.active)return next();res.sendFile(path.join(publicDir,'ebook.html'))});
 app.get('/api/public/ebook-page/:slug',(req,res)=>{const page=pageBySlug(req.params.slug);if(!page||!page.active)return res.status(404).json({error:'Landing page não disponível.'});const source=clean(req.query.src||req.query.source||req.query.utm_source,120)||'direto',campaign=clean(req.query.campaign||req.query.utm_campaign,120);db.prepare(`INSERT INTO ebook_page_visits(page_id,source,campaign)VALUES(?,?,?)`).run(page.id,source,campaign);res.set('Cache-Control','no-store');res.json({id:page.id,slug:page.slug,title:page.title,image_path:page.image_path||'/capa.webp'})});
 app.post('/api/public/ebook-leads',(req,res)=>{try{const page=pageById(req.body?.page_id)||pageBySlug(req.body?.slug)||defaultPage;if(!page||!page.active)return res.status(404).json({error:'Landing page não disponível.'});const name=clean(req.body?.name,120),email=clean(req.body?.email,180).toLowerCase(),company=clean(req.body?.company,180),source=clean(req.body?.source,120)||'direto',campaign=clean(req.body?.campaign,120),marketingConsent=req.body?.marketing_consent?1:0;if(!name)return res.status(400).json({error:'Indique o seu nome.'});if(!validEmail(email))return res.status(400).json({error:'Indique um email válido.'});let lead=db.prepare(`SELECT * FROM ebook_leads WHERE page_id=? AND lower(email)=lower(?) ORDER BY id DESC LIMIT 1`).get(page.id,email),token;if(lead){token=lead.token||crypto.randomBytes(24).toString('hex');db.prepare(`UPDATE ebook_leads SET name=?,company=?,source=?,campaign=?,marketing_consent=?,token=? WHERE id=?`).run(name,company,source,campaign,marketingConsent,token,lead.id)}else{token=crypto.randomBytes(24).toString('hex');db.prepare(`INSERT INTO ebook_leads(page_id,name,email,company,source,campaign,marketing_consent,token)VALUES(?,?,?,?,?,?,?,?)`).run(page.id,name,email,company,source,campaign,marketingConsent,token)}let prospect={created:false,already_exists:false};try{prospect=createProspectFromEbookLead({name,email,company,page,source,campaign})}catch(e){console.warn('[ebook] criar prospect:',e.message)}const downloadUrl=`${portal}/api/public/ebook-download/${encodeURIComponent(token)}`;try{const body=`Olá ${name},\n\nO seu ebook gratuito da DUIT está pronto.\n\nDescarregar: ${downloadUrl}\n\nBoa leitura,\nDUIT`,html=`<!doctype html><html><body style="margin:0;background:#f5f3ef;font-family:Arial,sans-serif"><table width="100%" cellpadding="0" cellspacing="0" style="padding:28px 14px"><tr><td align="center"><table width="600" cellpadding="0" cellspacing="0" style="max-width:600px;background:#fff;border-radius:14px;overflow:hidden"><tr><td style="background:#0a0a0a;padding:18px 30px"><img src="${portal}/logo-branco.png" width="135" alt="DUIT"></td></tr><tr><td style="height:4px;background:#ffd60a"></td></tr><tr><td style="padding:34px"><h1 style="font-size:26px;margin:0 0 14px">O seu ebook gratuito está pronto.</h1><p style="font-size:15px;line-height:1.6;color:#555">Obrigado, ${name}. Pode descarregar o ebook abaixo.</p><a href="${downloadUrl}" style="display:inline-block;background:#ffd60a;color:#111;text-decoration:none;font-weight:700;padding:13px 20px;border-radius:9px">Descarregar ebook gratuito</a></td></tr></table></td></tr></table></body></html>`;deliver(db,{to:email,subject:'O seu ebook gratuito DUIT',body,html,kind:'ebook_lead',force:true})}catch(e){console.warn('[ebook] email:',e.message)}res.json({ok:true,download_url:downloadUrl,prospect_created:!!prospect.created,prospect_already_exists:!!prospect.already_exists})}catch(e){console.warn('[ebook] lead:',e.message);res.status(500).json({error:'Não foi possível preparar o ebook. Tente novamente.'})}});
 app.get('/api/public/ebook-download/:token',(req,res)=>{try{const token=clean(req.params.token,100),lead=db.prepare(`SELECT l.id,l.page_id,p.pdf_path FROM ebook_leads l LEFT JOIN ebook_pages p ON p.id=l.page_id WHERE l.token=?`).get(token);if(!lead)return res.status(404).send('Ligação inválida ou expirada.');db.prepare(`UPDATE ebook_leads SET first_downloaded_at=COALESCE(first_downloaded_at,datetime('now')),last_downloaded_at=datetime('now'),download_count=download_count+1 WHERE id=?`).run(lead.id);res.set('Cache-Control','no-store');res.redirect(302,lead.pdf_path||'/ebook-duit.pdf')}catch(e){res.status(500).send('Não foi possível abrir o ebook.')}});
 app.get('/api/public/ebook-asset-file/:file',(req,res)=>{const file=path.join(assetDir,path.basename(req.params.file));if(!fs.existsSync(file))return res.sendStatus(404);res.sendFile(file)});
 app.get('/api/crm/ebook-pages',requireAdmin,(req,res)=>res.json(db.prepare(`SELECT p.*,(SELECT COUNT(*) FROM ebook_page_visits v WHERE v.page_id=p.id) views,(SELECT COUNT(*) FROM ebook_leads l WHERE l.page_id=p.id) leads,(SELECT COALESCE(SUM(download_count),0) FROM ebook_leads l WHERE l.page_id=p.id) downloads FROM ebook_pages p ORDER BY is_favorite DESC,datetime(p.updated_at) DESC,p.id DESC`).all()));
 app.post('/api/crm/ebook-pages',requireAdmin,(req,res)=>{const title=clean(req.body?.title,220);if(!title)return res.status(400).json({error:'Indique um título.'});const hasFav=!!db.prepare(`SELECT 1 FROM ebook_pages WHERE is_favorite=1 LIMIT 1`).get();const r=db.prepare(`INSERT INTO ebook_pages(slug,title,image_path,pdf_path,active,is_favorite)VALUES(?,?,?,?,1,?)`).run(randomSlug(),title,'/capa.webp','/ebook-duit.pdf',hasFav?0:1);res.json(pageById(r.lastInsertRowid))});
 app.patch('/api/crm/ebook-pages/:id',requireAdmin,(req,res)=>{const p=pageById(req.params.id);if(!p)return res.status(404).json({error:'Landing page não encontrada.'});const title=req.body?.title!==undefined?clean(req.body.title,220):p.title,active=req.body?.active!==undefined?(req.body.active?1:0):p.active;if(!title)return res.status(400).json({error:'Indique um título.'});const tx=db.transaction(()=>{db.prepare(`UPDATE ebook_pages SET title=?,active=?,updated_at=datetime('now') WHERE id=?`).run(title,active,p.id);if(req.body?.favorite){db.prepare(`UPDATE ebook_pages SET is_favorite=0`).run();db.prepare(`UPDATE ebook_pages SET is_favorite=1,active=1,updated_at=datetime('now') WHERE id=?`).run(p.id)}});tx();res.json(pageById(p.id))});
 app.post('/api/crm/ebook-pages/:id/duplicate',requireAdmin,(req,res)=>{const p=pageById(req.params.id);if(!p)return res.status(404).json({error:'Landing page não encontrada.'});const r=db.prepare(`INSERT INTO ebook_pages(slug,title,image_path,pdf_path,active,is_favorite)VALUES(?,?,?,?,1,0)`).run(randomSlug(),`${p.title} — cópia`,p.image_path,p.pdf_path);res.json(pageById(r.lastInsertRowid))});
 const rawImage=express.raw({type:['image/png','image/jpeg','image/webp'],limit:'15mb'}),rawPdf=express.raw({type:'application/pdf',limit:'40mb'});
 app.post('/api/crm/ebook-pages/:id/upload/image',requireAdmin,rawImage,(req,res)=>{const p=pageById(req.params.id);if(!p)return res.status(404).json({error:'Landing page não encontrada.'});if(!Buffer.isBuffer(req.body)||!req.body.length)return res.status(400).json({error:'Imagem inválida.'});const ext=req.headers['content-type']==='image/png'?'.png':req.headers['content-type']==='image/jpeg'?'.jpg':'.webp',file=`ebook-${p.id}-cover-${Date.now()}${ext}`;fs.writeFileSync(path.join(assetDir,file),req.body);const url=`/api/public/ebook-asset-file/${file}`;db.prepare(`UPDATE ebook_pages SET image_path=?,updated_at=datetime('now') WHERE id=?`).run(url,p.id);res.json({ok:true,url})});
 app.post('/api/crm/ebook-pages/:id/upload/pdf',requireAdmin,rawPdf,(req,res)=>{const p=pageById(req.params.id);if(!p)return res.status(404).json({error:'Landing page não encontrada.'});if(!Buffer.isBuffer(req.body)||!req.body.length)return res.status(400).json({error:'PDF inválido.'});const file=`ebook-${p.id}-${Date.now()}.pdf`;fs.writeFileSync(path.join(assetDir,file),req.body);const url=`/api/public/ebook-asset-file/${file}`;db.prepare(`UPDATE ebook_pages SET pdf_path=?,updated_at=datetime('now') WHERE id=?`).run(url,p.id);res.json({ok:true,url})});
 app.get('/api/crm/ebook-leads',requireAdmin,(req,res)=>{const pageId=Number(req.query.page_id||0),sql=`SELECT l.id,l.page_id,p.title page_title,l.name,l.email,l.company,l.source,l.campaign,l.marketing_consent,l.created_at,l.first_downloaded_at,l.last_downloaded_at,l.download_count FROM ebook_leads l LEFT JOIN ebook_pages p ON p.id=l.page_id ${pageId?'WHERE l.page_id=?':''} ORDER BY datetime(l.created_at) DESC,l.id DESC`;res.json(pageId?db.prepare(sql).all(pageId):db.prepare(sql).all())});
 app.get('/api/crm/ebook-pages/:id/sources',requireAdmin,(req,res)=>res.json(db.prepare(`SELECT COALESCE(NULLIF(source,''),'direto') source,COALESCE(campaign,'') campaign,COUNT(*) visits FROM ebook_page_visits WHERE page_id=? GROUP BY source,campaign ORDER BY visits DESC`).all(Number(req.params.id))));
};