const db=require('./db');
const {requireAdmin}=require('./auth');
function setup(){const cols=db.prepare(`PRAGMA table_info(prospect_crm)`).all();if(!cols.some(c=>c.name==='proposal_template_id'))db.exec(`ALTER TABLE prospect_crm ADD COLUMN proposal_template_id INTEGER`);db.exec(`CREATE TABLE IF NOT EXISTS duit_start_prospect_orders (id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL UNIQUE,status TEXT NOT NULL DEFAULT 'briefing',price REAL NOT NULL DEFAULT 30,content_count INTEGER NOT NULL DEFAULT 3,business_description TEXT,communication_goal TEXT,audience TEXT,links TEXT,materials TEXT,exclusions TEXT,payment_method TEXT,payment_status TEXT NOT NULL DEFAULT 'pending',created_at TEXT DEFAULT (datetime('now')),updated_at TEXT DEFAULT (datetime('now')),FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS duit_start_prospect_contents (id INTEGER PRIMARY KEY AUTOINCREMENT,order_id INTEGER NOT NULL,position INTEGER NOT NULL,title TEXT,image_url TEXT,post_text TEXT,created_at TEXT DEFAULT (datetime('now')),FOREIGN KEY(order_id) REFERENCES duit_start_prospect_orders(id) ON DELETE CASCADE,UNIQUE(order_id,position));`);const oc=db.prepare(`PRAGMA table_info(duit_start_prospect_orders)`).all();if(!oc.some(c=>c.name==='preview_image_url'))db.exec(`ALTER TABLE duit_start_prospect_orders ADD COLUMN preview_image_url TEXT`);const standard=db.prepare(`SELECT id FROM proposal_templates WHERE key='standard'`).get();if(standard)db.prepare(`UPDATE prospect_crm SET proposal_template_id=? WHERE proposal_template_id IS NULL`).run(standard.id)}
function cfg(row){let c={};try{c=JSON.parse(row?.config||'{}')}catch(e){}return c}
module.exports=function(app){setup();app.post('/api/crm/prospects/:id/send-email',requireAdmin,(req,res,next)=>{try{const row=db.prepare(`SELECT email_sent_at FROM prospect_crm WHERE user_id=?`).get(req.params.id);if(row&&!row.email_sent_at){const t=Number(req.body?.proposal_template_id)||db.prepare(`SELECT id FROM proposal_templates WHERE is_default=1 AND is_active=1 ORDER BY id LIMIT 1`).get()?.id;if(t)db.prepare(`UPDATE prospect_crm SET proposal_template_id=? WHERE user_id=?`).run(t,req.params.id)}}catch(e){console.warn('[templates] assign prospect:',e.message)}next()});app.get('/api/public/prospect-template/:token',(req,res)=>{const p=db.prepare(`SELECT c.user_id,c.proposal_template_id,pt.key,pt.name,pt.type,pt.config FROM prospect_crm c LEFT JOIN proposal_templates pt ON pt.id=c.proposal_template_id JOIN users u ON u.id=c.user_id WHERE c.email_tracking_token=? AND u.is_prospect=1`).get(String(req.params.token||''));if(!p)return res.status(404).json({error:'Ligação inválida.'});const order=db.prepare(`SELECT id,status,price,content_count,payment_status FROM duit_start_prospect_orders WHERE user_id=?`).get(p.user_id)||null;res.json({template:{id:p.proposal_template_id,key:p.key||'standard',name:p.name||'Proposta Standard',type:p.type||'standard',config:cfg(p)},duit_start:order})});app.post('/api/public/prospect-duit-start/:token',(req,res)=>{const p=db.prepare(`SELECT c.user_id,pt.type,pt.config FROM prospect_crm c LEFT JOIN proposal_templates pt ON pt.id=c.proposal_template_id JOIN users u ON u.id=c.user_id WHERE c.email_tracking_token=? AND u.is_prospect=1`).get(String(req.params.token||''));if(!p)return res.status(404).json({error:'Ligação inválida.'});if(p.type!=='duit_start')return res.status(400).json({error:'DUIT Start não está disponível nesta proposta.'});const c=cfg(p),b=req.body||{},old=db.prepare(`SELECT id FROM duit_start_prospect_orders WHERE user_id=?`).get(p.user_id);if(old)return res.json({ok:true,id:old.id,status:'awaiting_payment'});const info=db.prepare(`INSERT INTO duit_start_prospect_orders (user_id,status,price,content_count,business_description,communication_goal,audience,links,materials,exclusions,payment_method) VALUES (?,'awaiting_payment',?,?,?,?,?,?,?,?,?)`).run(p.user_id,Number(c.startPrice||30),Number(c.contentCount||3),b.business_description||'',b.communication_goal||'',b.audience||'',b.links||'',b.materials||'',b.exclusions||'',b.payment_method||'');db.prepare(`UPDATE prospect_crm SET lead_status='interessado',updated_at=datetime('now') WHERE user_id=?`).run(p.user_id);res.status(201).json({ok:true,id:info.lastInsertRowid,status:'awaiting_payment'})});app.get('/api/duit-start-prospects',requireAdmin,(req,res)=>res.json(db.prepare(`SELECT o.*,u.name client_name,u.company client_company,u.email client_email,c.email_tracking_token FROM duit_start_prospect_orders o JOIN users u ON u.id=o.user_id LEFT JOIN prospect_crm c ON c.user_id=o.user_id ORDER BY o.created_at DESC`).all()));app.patch('/api/duit-start-prospects/:id',requireAdmin,(req,res)=>{const {status,payment_status,preview_image_url}=req.body||{};db.prepare(`UPDATE duit_start_prospect_orders SET status=COALESCE(?,status),payment_status=COALESCE(?,payment_status),preview_image_url=COALESCE(?,preview_image_url),updated_at=datetime('now') WHERE id=?`).run(status??null,payment_status??null,preview_image_url??null,req.params.id);res.json({ok:true})});app.put('/api/duit-start-prospects/:id/contents/:position',requireAdmin,(req,res)=>{const pos=Math.max(1,Math.min(3,Number(req.params.position)||1)),b=req.body||{};db.prepare(`INSERT INTO duit_start_prospect_contents (order_id,position,title,image_url,post_text) VALUES (?,?,?,?,?) ON CONFLICT(order_id,position) DO UPDATE SET title=excluded.title,image_url=excluded.image_url,post_text=excluded.post_text`).run(req.params.id,pos,b.title||`Conteúdo ${pos}`,'',b.post_text||'');const o=db.prepare(`SELECT preview_image_url FROM duit_start_prospect_orders WHERE id=?`).get(req.params.id),n=db.prepare(`SELECT COUNT(*) c FROM duit_start_prospect_contents WHERE order_id=? AND TRIM(COALESCE(post_text,''))!=''`).get(req.params.id).c;if(o?.preview_image_url&&n>=3)db.prepare(`UPDATE duit_start_prospect_orders SET status='ready',updated_at=datetime('now') WHERE id=?`).run(req.params.id);res.json({ok:true})});app.get('/api/public/prospect-duit-start/:token',(req,res)=>{const o=db.prepare(`SELECT o.id,o.status,o.price,o.content_count,o.payment_status,o.preview_image_url,u.name client_name,u.company client_company FROM duit_start_prospect_orders o JOIN users u ON u.id=o.user_id JOIN prospect_crm c ON c.user_id=o.user_id WHERE c.email_tracking_token=?`).get(String(req.params.token||''));if(!o)return res.status(404).json({error:'DUIT Start ainda não iniciado.'});const contents=db.prepare(`SELECT position,title,post_text FROM duit_start_prospect_contents WHERE order_id=? ORDER BY position`).all(o.id);res.json({...o,contents,can_convert:o.status==='ready'&&!!o.preview_image_url&&contents.filter(c=>String(c.post_text||'').trim()).length>=3})});};
