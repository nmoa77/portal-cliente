/* DUIT — camada de template da proposta pública */
(()=>{
  const params=new URLSearchParams(location.search), token=params.get('token');
  if(!token)return;
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let template=null;

  function css(){if(document.getElementById('duit-template-css'))return;const s=document.createElement('style');s.id='duit-template-css';s.textContent=`
    .duit-start-offer{margin-top:24px;padding:26px;border:1px solid #ead98a;border-radius:16px;background:linear-gradient(135deg,#fffdf3,#fff8d4)}
    .duit-start-tag{display:inline-block;font-size:10px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;background:#ffd60a;padding:6px 9px;border-radius:999px;margin-bottom:10px}
    .duit-start-offer h2{font-family:'Clash Display',sans-serif;font-size:25px;margin:0 0 7px}.duit-start-offer p{font-size:13px;line-height:1.55;color:#5f5a50;margin:0 0 15px}
    .duit-start-features{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:16px 0}.duit-start-feature{font-size:13px;font-weight:600}.duit-start-price{font-family:'Clash Display',sans-serif;font-size:36px;font-weight:700;margin:8px 0}.duit-start-note{font-size:11px!important;margin-top:12px!important}
    .duit-start-modal{position:fixed;inset:0;background:rgba(10,10,10,.65);z-index:500;display:none;align-items:center;justify-content:center;padding:18px}.duit-start-modal.show{display:flex}.duit-start-box{background:#fff;width:min(620px,100%);max-height:92vh;overflow:auto;border-radius:18px;padding:28px}.duit-start-box h2{font-family:'Clash Display';font-size:25px;margin:0 0 5px}.duit-start-box .intro{font-size:13px;color:#6b6862;margin-bottom:20px}.duit-start-field{margin:13px 0}.duit-start-field label{display:block;font-size:12px;font-weight:700;margin-bottom:5px}.duit-start-field input,.duit-start-field textarea,.duit-start-field select{width:100%;box-sizing:border-box;border:1px solid #ddd9d0;border-radius:9px;padding:11px 12px;font:inherit}.duit-start-field textarea{min-height:72px;resize:vertical}.duit-start-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:20px}.duit-start-success{text-align:center;padding:22px 4px}.duit-start-success h2{font-size:28px}
    @media(max-width:600px){.duit-start-features{grid-template-columns:1fr}.duit-start-offer{padding:20px}}
  `;document.head.appendChild(s)}

  async function init(){
    try{const r=await fetch('/api/public/quote-template/'+encodeURIComponent(token));if(!r.ok)return;const d=await r.json();template=d.template;if(template?.type!=='duit_start')return;css();waitForQuote(d)}catch(e){console.warn('proposal template',e)}
  }
  function waitForQuote(data){let tries=0;const t=setInterval(()=>{tries++;const card=document.querySelector('.pq-card');if(card&&document.querySelector('.pq-totals')){clearInterval(t);mountOffer(data)}else if(tries>80)clearInterval(t)},100)}
  function mountOffer(data){if(document.getElementById('duitStartOffer'))return;const cfg=template.config||{},price=Number(cfg.startPrice||30),count=Number(cfg.contentCount||3);const el=document.createElement('section');el.id='duitStartOffer';el.className='duit-start-offer';el.innerHTML=`<span class="duit-start-tag">Ainda não quer assumir uma mensalidade?</span><h2>Experimente primeiro.</h2><p>Conheça a forma como a DUIT pensa, cria e comunica antes de decidir por um acompanhamento mensal.</p><div class="duit-start-features"><div class="duit-start-feature">✓ ${count} conteúdos para as suas redes</div><div class="duit-start-feature">✓ Design + texto incluídos</div><div class="duit-start-feature">✓ Apenas para visualizar</div><div class="duit-start-feature">✓ Sem fidelização</div></div><div class="duit-start-price">${price.toLocaleString('pt-PT')} €</div><button class="btn btn-yellow" id="duitStartBtn">Quero experimentar</button><p class="duit-start-note">Se depois avançar para um plano mensal, descontamos os ${Number(cfg.discountOnFirstMonth||price).toLocaleString('pt-PT')} € na primeira mensalidade.</p>`;
    const totals=document.querySelector('.pq-totals');totals.insertAdjacentElement('afterend',el);document.getElementById('duitStartBtn').onclick=openStart;
    if(data.duit_start) document.getElementById('duitStartBtn').textContent='Continuar DUIT Start';
  }
  function openStart(){let m=document.getElementById('duitStartModal');if(!m){m=document.createElement('div');m.id='duitStartModal';m.className='duit-start-modal';m.innerHTML=`<div class="duit-start-box"><div id="duitStartFormWrap"><div class="duit-start-tag">DUIT Start · 30 €</div><h2>Vamos criar os seus 3 conteúdos.</h2><p class="intro">Precisamos apenas do essencial para perceber o seu negócio. Não há reunião nem compromisso mensal.</p><form id="duitStartForm"><div class="duit-start-field"><label>1. O que faz a sua empresa?</label><textarea name="business_description" required></textarea></div><div class="duit-start-field"><label>2. O que gostaria de comunicar neste momento?</label><textarea name="communication_goal" placeholder="Serviço, produto, novidade, campanha..."></textarea></div><div class="duit-start-field"><label>3. Para quem comunica?</label><input name="audience" placeholder="Descreva o seu cliente habitual"></div><div class="duit-start-field"><label>4. Onde podemos conhecer melhor a empresa?</label><input name="links" placeholder="Website, Instagram, Facebook..."></div><div class="duit-start-field"><label>5. Materiais disponíveis</label><textarea name="materials" placeholder="Logótipo, fotografias, links para Drive/WeTransfer..."></textarea></div><div class="duit-start-field"><label>6. Há algo que não queira ver na comunicação?</label><textarea name="exclusions" placeholder="Opcional"></textarea></div><div class="duit-start-field"><label>Como prefere pagar?</label><select name="payment_method"><option value="mbway">MB WAY</option><option value="transferencia">Transferência bancária</option></select></div><div class="duit-start-actions"><button type="button" class="btn btn-ghost" id="duitStartClose">Agora não</button><button class="btn btn-yellow">Continuar · 30 €</button></div></form></div></div>`;document.body.appendChild(m);m.querySelector('#duitStartClose').onclick=()=>m.classList.remove('show');m.querySelector('#duitStartForm').onsubmit=submitStart;m.onclick=e=>{if(e.target===m)m.classList.remove('show')}}m.classList.add('show')}
  async function submitStart(e){e.preventDefault();const btn=e.submitter;btn.disabled=true;btn.textContent='A guardar…';const body=Object.fromEntries(new FormData(e.target).entries());try{const r=await fetch('/api/public/duit-start/'+encodeURIComponent(token),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Erro');document.getElementById('duitStartFormWrap').innerHTML=`<div class="duit-start-success"><div style="font-size:42px">✓</div><h2>Pedido recebido.</h2><p>Já temos o briefing do seu DUIT Start. O passo seguinte é o pagamento de 30 €. A equipa DUIT irá enviar-lhe os dados de pagamento.</p><button class="btn btn-yellow" onclick="document.getElementById('duitStartModal').classList.remove('show')">Fechar</button></div>`;document.getElementById('duitStartBtn').textContent='DUIT Start iniciado'}catch(err){alert(err.message);btn.disabled=false;btn.textContent='Continuar · 30 €'}}
  init();
})();
