(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function nav(){const n=document.getElementById('nav');if(!n||n.querySelector('[data-duit-start]'))return;const t=n.querySelector('[data-duit-templates]');if(!t)return;const b=document.createElement('button');b.className='nav-item';b.dataset.duitStart='1';b.innerHTML='<svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v20M2 12h20"/></svg><span>DUIT Start</span>';b.onclick=view;t.insertAdjacentElement('afterend',b)}new MutationObserver(nav).observe(document.documentElement,{childList:true,subtree:true});nav();async function view(){document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));document.querySelector('[data-duit-start]')?.classList.add('active');const m=document.getElementById('main');m.innerHTML='<div class="empty">A carregar…</div>';try{const rows=await api('/api/duit-start-prospects');m.innerHTML=`<div class="page-head"><div><div class="eyebrow">Experiência de entrada</div><h1>DUIT Start</h1><p class="lede">Briefing → pagamento → produção → 3 conteúdos para visualização → conversão para mensalidade.</p></div></div>${rows.length?`<div class="card" style="padding:0"><div class="table-wrap"><table><thead><tr><th>Empresa</th><th>Estado</th><th>Pagamento</th><th>Entrada</th><th></th></tr></thead><tbody>${rows.map(o=>`<tr><td><strong>${esc(o.client_company||o.client_name)}</strong><div style="font-size:11px;color:var(--muted)">${esc(o.client_email)}</div></td><td>${esc(label(o.status))}</td><td>${esc(o.payment_status)}</td><td>30 €</td><td><button class="btn btn-sm btn-ghost" onclick="window.dsManage(${o.id})">Abrir</button></td></tr>`).join('')}</tbody></table></div></div>`:'<div class="card"><div class="empty">Ainda não existem pedidos DUIT Start.</div></div>'}`}catch(e){m.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}function label(s){return({awaiting_payment:'Aguarda pagamento',production:'Em produção',ready:'Pronto para visualizar',converted:'Convertido'})[s]||s}window.dsManage=async id=>{const rows=await api('/api/duit-start-prospects'),o=rows.find(x=>+x.id===+id);if(!o)return;const m=document.getElementById('main');m.innerHTML=`<div class="page-head"><div><div class="eyebrow">DUIT Start</div><h1>${esc(o.client_company||o.client_name)}</h1><p class="lede">O cliente apenas visualiza os conteúdos. Não existem botões de aprovação ou download.</p></div><button class="btn btn-ghost" id="dsBack">← Voltar</button></div><div class="grid g-2"><div class="card"><h3>Briefing</h3>${[['O que faz',o.business_description],['O que quer comunicar',o.communication_goal],['Público',o.audience],['Links',o.links],['Materiais',o.materials],['Evitar',o.exclusions]].map(x=>`<div style="padding:10px 0;border-bottom:1px solid var(--line-2)"><div class="eyebrow">${x[0]}</div><div>${esc(x[1]||'—')}</div></div>`).join('')}<div style="margin-top:16px"><button class="btn btn-yellow btn-sm" id="dsPaid">Pagamento recebido</button></div></div><div class="card"><h3>3 conteúdos</h3><p class="lede">Imagem em cima, texto em baixo — exatamente como o prospect irá ver.</p>${[1,2,3].map(n=>`<div style="padding:13px 0;border-bottom:1px solid var(--line)"><strong>Conteúdo ${n}</strong><div class="field"><label>URL da imagem</label><input id="dsi${n}" placeholder="https://..."></div><div class="field"><label>Texto</label><textarea id="dst${n}" rows="4"></textarea></div><button class="btn btn-sm btn-ghost" onclick="window.dsSave(${id},${n})">Guardar</button></div>`).join('')}</div></div>`;document.getElementById('dsBack').onclick=view;document.getElementById('dsPaid').onclick=async()=>{await api(`/api/duit-start-prospects/${id}`,{method:'PATCH',body:JSON.stringify({payment_status:'paid',status:'production'})});alert('Pagamento confirmado. Pode iniciar a produção.')};};window.dsSave=async(id,n)=>{await api(`/api/duit-start-prospects/${id}/contents/${n}`,{method:'PUT',body:JSON.stringify({title:`Conteúdo ${n}`,image_url:document.getElementById('dsi'+n).value,post_text:document.getElementById('dst'+n).value})});alert(`Conteúdo ${n} guardado.`)};})();
